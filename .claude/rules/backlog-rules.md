# 백로그 파일 규칙

이 프로젝트의 백로그 파일(기본 `docs/backlog.md`)을 읽거나 쓸 때는 `backlog` 스킬(`.claude/skills/backlog/SKILL.md`)의 형식(미완료/완료 두 섹션, `BL-NN`/`BC-NN` ID, 완료 시 두 줄 요약)을 따른다. 새 항목 추가, 완료 처리, 새 백로그 파일 생성은 이 스킬을 통해 수행한다.
