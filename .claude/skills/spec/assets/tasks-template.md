# Implementation Plan: <spec-name>

## Overview

<몇 단계(Step)로 나눠 진행하는지 1~2문장 요약. design.md의 어떤 부분이 어느 Step에 대응하는지.>

## Checkbox States

작성 시점의 모든 태스크는 `[ ]`다 (SKILL.md 참고). `[ ]`→작성됨(실행 범위 아님), `[-]`→대기(실행 시작 시 이번 범위 전체를 일괄 마킹), `[~]`→진행 중(시작하는 즉시 개별 갱신), `[x]`→완료(검증까지 마친 뒤 개별 갱신). 전환은 항상 `[ ]`→`[-]`(일괄)→`[~]`→`[x]`(개별) 순서다.

## Definition of Done

모든 리프 태스크는 `_Requirements:` 줄 바로 위에 `**완료 조건:**`을 명시한다 — 실행 후 참/거짓으로 확인 가능한 조건이어야 한다(특정 명령의 성공 종료, 특정 테스트의 통과, 특정 파일/산출물의 존재 등). "적절히 동작한다", "잘 반영된다" 같은 주관적 서술은 완료 조건이 아니다. 이 조건은 그 태스크를 `[~]`→`[x]`로 갱신하기 전에 실제로 확인해야 하는 것이다(SKILL.md "실행 중 상태 갱신" 참고).

---

## Tasks

- [ ] 1. Step 1: <제목>

  - [ ] 1.1 <순수 설정/빌드/인프라 작업 — TDD 구조 불필요>
    - <구체적 작업 — 어떤 파일을 어떻게 바꾸는지>
    - **완료 조건:** <예: `./gradlew compileJava`가 오류 없이 종료한다>
    - _Requirements: 1.1, 1.2_

  - [ ] 1.2 <비즈니스 로직 구현 작업 — Red → Green → Refactor로 하위 단계를 쪼갠다>
    - **Red:** Requirement 2.1의 Acceptance Criteria(또는 design.md의 Property 1)를 근거로 실패하는 테스트를 먼저 작성한다.
    - **Green:** 테스트를 통과시키는 최소 구현을 한다.
    - **Refactor:** 테스트가 계속 그린인 상태를 유지하며 구조를 정리한다.
    - **완료 조건:** Green에서 작성한 테스트가 Refactor 이후에도 계속 통과한다.
    - _Requirements: 2.1_

  - [ ] 1.3 <design.md에서 도출된 Correctness Property 검증 — jqwik 사용>
    - jqwik 의존성이 프로젝트에 없으면 먼저 추가한다 (`testImplementation` 등).
    - **Property 1: <이름>** — <속성 요약. jqwik `@Property` + `@ForAll`로 작성, AutoParams로 대체하지 않는다>
    - **Validates: Requirements 2.1**
    - **완료 조건:** jqwik property 테스트가 지정된 반복 횟수만큼 실패 없이 통과한다.
    - _Requirements: 2.1_

  - [ ] 1.4 Step 1 검증
    - <검증 명령어/확인 사항>
    - **완료 조건:** 위 검증 명령어가 전부 성공 종료하고, 확인 사항이 모두 충족된다.
    - _Requirements: 5.1_

- [ ] 2. Checkpoint — Step 1 완료 확인
  - <무엇을 확인하는가. 문제가 있으면 작업을 멈추고 사용자에게 질문한다.>

- [ ] 3. Step 2: <제목>
  - [ ] 3.1 ...
    - **완료 조건:** ...
    - _Requirements: ..._

<반복: Step N개, Step 사이마다 Checkpoint 삽입>

- [ ] N. Final Checkpoint — 전체 통합 검증
  - <최종 확인 사항>
  - _Requirements: ..._
  - 모든 검증이 통과하면 다음 태스크로 진행. 문제가 있으면 작업을 멈추고 사용자에게 질문한다.

- [ ] N+1. 관련 문서 업데이트 (모든 스펙에 필수 — 생략하지 않는다)
  - tasks.md를 작성하는 지금(실행 전) `references/spec-promotion-rules.md` 기준으로 승격 대상을 판단해 아래 서브태스크로 구체화한다 — 실행 시점에는 다시 판단하지 않고 그대로 수행한다. 개수는 0개(전부 "해당 없음")부터 여러 개까지 스펙마다 다르다.
  - [ ] N+1.1 <구체적 문서/항목, 예: "specs/_baseline.md에 G-3 추가">
    - **완료 조건:** <예: specs/_baseline.md의 Global Rules 표에 G-3 행이 실제로 추가되어 있다>
  - [ ] N+1.2 <구체적 문서/항목, 예: "CLAUDE.md의 Testing Notes 절 갱신">
    - **완료 조건:** <예: CLAUDE.md의 해당 절 내용이 갱신된 내용을 반영한다>
  - <승격 대상이 전혀 없으면 서브태스크 없이 이 태스크 자체를 "해당 없음"으로 완료 표시한다.>
  - 이 태스크가 완료(`[x]`)되면 스펙 전체가 완료된 것이다. 완료된 스펙은 이후 편집하지 않는다 — 새로운 필요가 생기면 새 스펙을 만든다.

---

## Notes

<폴백 전략, 주의사항. 예: 특정 라이브러리 버전 업에서 컴파일 오류 시 대응, 자격증명/시크릿 취급 주의 등. 없으면 이 섹션을 생략한다.>
